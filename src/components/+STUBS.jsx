import * as React from 'react';

/**
 * Placeholder stubs for components planned in upcoming milestones
 */

export const FieldCalc = ({ className, ...props }) => (
  <div className={className} {...props} />
);

export const ToolsPokeProvider = ({ children }) => (
  <>{children}</>
);

export const PokeCalc = ({ className, ...props }) => (
  <div className={className} {...props} />
);
